pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

enableFeaturePreview("VERSION_CATALOGS")
enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://raw.githubusercontent.com/kotlin-graphics/mary/master"
        }
    }
}
rootProject.name = "composable-sheep"
include ':sheepCanvas'
include ':design'
include ':canvasExtensions'
include ':sheep'
include ':sheepAnimations'
include ':canvasSketch'
